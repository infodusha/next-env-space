/**
 * Stops a prefetch of the routes below at this segment: everything under the
 * boundary is deferred to the navigation itself, the page module included.
 */
export default function Loading() {
  return <p>loading…</p>;
}
