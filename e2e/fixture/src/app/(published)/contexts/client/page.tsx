import { ClientContextsView } from "@/components/client-contexts-view";

/** The client rows of the README table, below the layout that renders `<ClientEnvScript />`. */
export default function ClientContextsPage() {
  return (
    <main>
      <ClientContextsView />
    </main>
  );
}
